import 'package:flutter/material.dart';

class ElegantLoader extends StatelessWidget {
  const ElegantLoader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  CircularProgressIndicator();
  }
}
